# Title

Title

## Usage

``` r
rebelLMM(
  fixedEffects,
  normalizedCounts = NULL,
  colData = NULL,
  pseudoBulk,
  subjectVariable,
  sampleVariable = NULL,
  REML = TRUE,
  parallel = FALSE,
  cores = 2,
  outputFits = FALSE
)
```

## Arguments

- outputFits:
